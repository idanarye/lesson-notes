<html>
    <head>
    </head>
    <body>
        <h1>{{basedir}}</h1>
        {{#each pdfs}}
            <h2>{{title}}</h2>
            {{#each items}}
                <ul>
                    <li>
                        <a href="{{file}}" target="_blank">
                            {{date}}
                        </a>
                    </li>
                </ul>
            {{/each}}
        {{/each}}
    </body>
</html>
