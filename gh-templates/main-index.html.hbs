<html>
    <head>
    </head>
    <body>
        <ul>
            {{#each this}}
                <li>
                    <a href="{{basedir}}/index.html">
                        {{basedir}}
                    </a>
                    ({{from_date}} to {{to_date}})
                </li>
            {{/each}}
        </ul>
    </body>
</html>
